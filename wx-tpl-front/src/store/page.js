const state = {
    // 页面数据缓存
    cache: {},
    // 页面滚动位置记录
    savedPosition: {},
    // 进入当前页面的 action
    // push || replace || back || forward
    action: '',
}
const getters = {
}
const mutations = {
}
const actions = {
}

export default {
    state,
    getters,
    mutations,
    actions
}
