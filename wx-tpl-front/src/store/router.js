const state = {
    // from
    from: '',
    // to
    to: '',
    // 是否正在路由
    isRouteing: false,
}
const getters = {
}
const mutations = {
}
const actions = {
}

export default {
    state,
    getters,
    mutations,
    actions
}
