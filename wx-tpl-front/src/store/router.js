const state = {
    // from
    from: '',
    // to
    to: '',
    // 是否正在路由
    isRouting: false,
    // 进入当前页面的 action
    // push || replace || back || forward
    action: '',
}
const getters = {
}
const mutations = {
}
const actions = {
}

export default {
    state,
    getters,
    mutations,
    actions
}
