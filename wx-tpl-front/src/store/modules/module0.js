// import Init from '@/init'

// const { $post: Post, $get: Get } = Init.protoData;

const state = {
}
const getters = {
}
const mutations = {
}
const actions = {
}

export default {
	state,
	getters,
	mutations,
	actions
}
